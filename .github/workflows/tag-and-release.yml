name: tag and release

on:
  push:
    branches:
      - main
    paths:
      - package.json

jobs:
  tag-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: butlerlogic/action-autotag@stable
        id: autotag
        with:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          strategy: package
          tag_prefix: "v"
          commit_message_template: ""
      - run: gh release create ${{steps.autotag.tag}} -R ${{github.repository}} --generate-notes -t "Release ${{steps.autotag.tag}}"
        env: 
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
